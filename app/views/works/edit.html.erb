<h1>ワークを編集する</h1>

<div class="navigation-space">
  <hr>
  <hr>
</div>

<%= render 'error_message', work: @work %>

<%= render 'form', diary: @diary, work: @work, templates: @templates, edit_flag: true %>