<div class="ml-2">
  <h1>Make New Template</h1>
</div>

<div class="mx-2">
  <%= render 'shared/error_message', posted_val: @template %>
  <%= render 'form', template: @template, diary_id: @diary_id %>
</div>